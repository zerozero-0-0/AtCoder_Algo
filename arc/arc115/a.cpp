#include <bits/stdc++.h>
using namespace std;
// #include <atcoder/all>
// using namespace atcoder;

int main() {
    int n, m;
    cin >> n >> m;
    vector<string> s(n);
    for (auto &i : s) cin >> i;

    
}
